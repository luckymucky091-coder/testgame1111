update() {

    const left =
        keys["a"] ||
        keys["arrowleft"] ||
        touch.left;

    const right =
        keys["d"] ||
        keys["arrowright"] ||
        touch.right;

    const jump =
        keys["w"] ||
        keys["arrowup"] ||
        keys[" "] ||
        touch.jump;

    // let left =
keys["a"]||
keys["arrowleft"]||
touch.left;

let right =
keys["d"]||
keys["arrowright"]||
touch.right;

let jump =
keys[" "]||
keys["w"]||
keys["arrowup"]||
touch.jump;
}
